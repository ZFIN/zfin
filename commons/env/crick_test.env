#!/local/bin/tcsh

setenv ANT_OPTS "-Xms256m -Xmx4096m -Xverify:none"
setenv CATALINA_BASE "/private/etc/tomcat/crick"
setenv CATALINA_HOME "/private/apps/tomcat"
setenv CATALINA_PID "/private/etc/tomcat/crick/catalina_pid"
setenv DBNAME "crickdb"
setenv DOMAIN_NAME "istest.zfin.org"
setenv DOWNLOAD_DIRECTORY "/research/zunloads/download-files/crickdb"
setenv GC_LOGGING_OPTS "-verbose:gc -verbose:sizes -XX:+PrintGCDetails -XX:+PrintGCDateStamps -XX:+PrintTenuringDistribution -Xloggc:/private/etc/tomcat/crick/logs/gc.log"
setenv IMAGE_LOAD "/imageLoadUp"
setenv INDEXER_UNLOAD_DIRECTORY "/research/zunloads/indexes/crickdb"
setenv INDEXER_WIKI_HOSTNAME "devwiki.zfin.org"
setenv INDEXER_WIKI_PASSWORD "dan1orer1o"
setenv INDEXER_WIKI_USERNAME "webservice"
setenv INFORMIXDIR "/private/apps/Informix/informix"
setenv INFORMIXSERVER "waldo"
setenv INFORMIXSQLHOSTS "/private/apps/Informix/informix/etc/sqlhosts"
setenv INFORMIX_PORT "2002"
setenv INSPECTLET_ID "0000000000"
setenv INSTANCE "crick_test"
setenv JAVA_HOME "/private/apps/java"
setenv JAVA_OPTS "-Xverify:none"
setenv JENKINS_HOME "/research/zcentral/www_homes/crick/server_apps/jenkins/jenkins-home"
setenv JPDA_ADDRESS "9338"
setenv LD_LIBRARY_PATH "/private/apps/Informix/informix/lib:/private/apps/Informix/informix/lib/esql"
setenv LOADUP_FULL_PATH "/private/ZfinLinks/loadUp"
setenv MUTANT_NAME "crick_test"
setenv ONCONFIG "onconfig"
setenv PGDATABASE ""
setenv SOLR_HOME "/research/zcentral/www_homes/crick/server_apps/solr"
setenv SOLR_PORT "9538"
setenv SOURCEROOT "/research/zusers/crick/ZFIN_WWW_trunk"
setenv SQLHOSTS_HOST "zygotix.zfin.org"
setenv TARGETCGIBIN "crick"
setenv TARGETFTPROOT "/research/zcentral/ftp"
setenv TARGETROOT "/research/zcentral/www_homes/crick"
setenv USER "informix"
setenv WEBHOST_BLASTDB_TO_COPY "/research/crick/blastdb"

# Define SVNROOT
if("${INFORMIXSERVER}" == "wildtype") then
    if ("`/local/bin/hostname`" != "helix") then
      echo "WARNING: Not currently logged in to helix."
    endif
    setenv SVNROOT file:///research/zcentral/Vault/SVNROOT ;
    if (! $?PREINFORMIXPATH) then
        setenv PREINFORMIXPATH ${PATH}
    endif

    # now that commons exists on helix, include it as well.
    setenv PATH /private/bin:${INFORMIXDIR}/bin:/private/ZfinLinks/Commons/bin:${PREINFORMIXPATH}
    if ($?PREINFORMIX_LD_PATH) then
        setenv LD_LIBRARY_PATH ${PREINFORMIX_LD_PATH}:${INFORMIXDIR}/lib:${INFORMIXDIR}/lib/esql
    else
        setenv LD_LIBRARY_PATH ${INFORMIXDIR}/lib:${INFORMIXDIR}/lib/esql
    endif
    # Set PATH variables and prompt using the standard variables
    # First check if PREINFORMIXPATH IS SET
    if (! $?PREINFORMIXPATH) then
        setenv PREINFORMIXPATH ${PATH}
    endif
    setenv PATH /private/bin:${INFORMIXDIR}/bin:/private/ZfinLinks/Commons/bin:${PREINFORMIXPATH}
    if ($?PREINFORMIX_LD_PATH) then
        setenv LD_LIBRARY_PATH ${PREINFORMIX_LD_PATH}:${INFORMIXDIR}/lib:${INFORMIXDIR}/lib/esql
    else
        setenv LD_LIBRARY_PATH ${INFORMIXDIR}/lib:${INFORMIXDIR}/lib/esql
    endif
    setenv SVNROOT svn+ssh://${USER}@zygotix.zfin.org/research/zcentral/Vault/SVNROOT
    # Set PATH variables and prompt using the standard variables
    # First check if PREINFORMIXPATH IS SET
    if (! $?PREINFORMIXPATH) then
        setenv PREINFORMIXPATH ${PATH}
    endif
    setenv PATH /private/bin:${INFORMIXDIR}/bin:/private/ZfinLinks/Commons/bin:${PREINFORMIXPATH}
    if ($?PREINFORMIX_LD_PATH) then
        setenv LD_LIBRARY_PATH ${PREINFORMIX_LD_PATH}:${INFORMIXDIR}/lib:${INFORMIXDIR}/lib/esql
    else
        setenv LD_LIBRARY_PATH ${INFORMIXDIR}/lib:${INFORMIXDIR}/lib/esql
    endif
    setenv SVNROOT svn+ssh://${USER}@zygotix.zfin.org/research/zcentral/Vault/SVNROOT
else
    # Set PATH variables and prompt using the standard variables
    # First check if PREINFORMIXPATH IS SET
    if (! $?PREINFORMIXPATH) then
        setenv PREINFORMIXPATH ${PATH}
    endif
    setenv PATH /private/bin:${INFORMIXDIR}/bin:/private/ZfinLinks/Commons/bin:${PREINFORMIXPATH}
    if ($?PREINFORMIX_LD_PATH) then
        setenv LD_LIBRARY_PATH ${PREINFORMIX_LD_PATH}:${INFORMIXDIR}/lib:${INFORMIXDIR}/lib/esql
    else
        setenv LD_LIBRARY_PATH ${INFORMIXDIR}/lib:${INFORMIXDIR}/lib/esql
    endif
    setenv SVNROOT svn+ssh://${USER}@zygotix.zfin.org/research/zcentral/Vault/SVNROOT
endif


setenv PATH /private/apps/groovy/bin:$PATH

setenv PATH /opt/postgres/postgresql/bin:/opt/postrgres/postgresql/include:$PATH

# Prompt
test ${?prompt} -eq 1 && set prompt = "${MUTANT_NAME} %c2$Prompt"

if ($user == 'informix') then
  umask 022
endif

