#!/local/bin/tcsh
#
# This script should be sourced to set several environment variables
# that are required by the ZFIN Makefiles.
#
# This script sets those variables for the embryonix preproduction/sandbox
# ZFIN test server, almost.zfin.org
# 
# define Makefile environment variables.
DBNAME=zfindb
MUTANT_NAME=zfin.org
DEFAULT_EMAIL=informix@zfin.org
USER=informix


MACHINE_NAME=watson.zfin.org
HOSTNAME=watson.zfin.org

#define Tomcat ports
SHUTDOWNPORT=2007
NON_SECUREPORT=2008
SECUREPORT=8443
DEBUGPORT=5000
JENKINS_PORT=9399

#The following defaults depend on these
INSTANCE=watson
TARGETFTPROOT=/research/zprod/ftp

include=all-defaults.properties
#FIXME: Create linux-prod-defaults.properties to replace overrides below
include=linux-vm-defaults.properties
# it's possible that we need this, but probably not...
#include=blast-fallback-overrides.properties 

#This gets changed in zfinproperties-default.properties - change back
#I think this is the only case where INSTANCE!=MUTANT_NAME
INSTANCE=watson

# override variables
DOMAIN_NAME=zfin.org
WEBDRIVER_LOC=cgi-bin
#TARGETCGIBIN=cgi-bin

WAREHOUSE_DUMP_DIR=/research/zunloads/databases/watson

RUN_QUARTZ_JOBS=false

LOG4J_FILE=production-site.log4j.xml

#FIXME: need production blast settings before RTP 
#FIXME: need production wiki settings before RTP
#FIXME: need production DOWNLOAD_DIRCTORY before RTP
