<HTML>
<head>
<META HTTP-EQUIV="EXPIRES" CONTENT="Wed Sep 11 09:35:25 PDT 1996">
<TITLE>ZFIN Add a new COMPANY record</TITLE>
</head>


<?MISQL SQL="select WebExplode(object,'page_title=New Company&permission=root') from webPages where ID='aa-secure_navigation.apg';">$1<?/MISQL>

<?MISQL SQL="select WebExplode(object,'') from webPages where ID='aa-htmlpageheader.apg';">$1<?/MISQL>

<?MIBLOCK COND="$(NC,$AUTHORIZED,false)">

  <!-- THIS IS A VERY TRICKY PAGE. The main challenge lies in the fact that users
  will be "building up" a submission, adding pubs to it, selecting affected gene,
  etc etc. In other words, they may engage in lots of "side processes" before they
  have built up their final submission and press the "submit" button.
  -->


  <!-- Pass in any variables you want the form to be initialized with on the command-line. You will need to modify setup-new-company to avoid blasting these values however.
  -->

<?MIVAR COND="$(NXST,$temp_oid)" NAME=$temp_oid><?/MIVAR>

  <?MIVAR>
    <FORM METHOD=POST ACTION="/<!--|WEBDRIVER_PATH_FROM_ROOT|-->">
    <INPUT type=hidden NAME=temp_oid VALUE=$temp_oid>
    <INPUT type=hidden NAME=MIval VALUE="aa-process-company.apg">
  <?/MIVAR>


  <h1 align=center> Add a new COMPANY to ZFIN</h1>

<?MIVAR COND="$(NXST,$name)" NAME=$name><?/MIVAR>
<?MIVAR COND="$(NXST,$address)" NAME=$address><?/MIVAR>
<?MIVAR COND="$(NXST,$phone)" NAME=$phone><?/MIVAR>
<?MIVAR COND="$(NXST,$fax)" NAME=$fax><?/MIVAR>
<?MIVAR COND="$(NXST,$email)" NAME=$email><?/MIVAR>
<?MIVAR COND="$(NXST,$url)" NAME=$url><?/MIVAR>
<?MIVAR COND="$(NXST,$contact_person)" NAME=$contact_person><?/MIVAR>


  <p>
  The following information is <B>REQUIRED</B> to create the new COMPANY record. Please enter the requested information in the boxes below, and then  click on the "SUBMIT" button.


  <p>
  <b>Full Name:</b><br>
  This field should hold the official name of the company. For example, "Genetech Inc". 
  <INPUT TYPE=text <?MIVAR COND=$(NC,$name,NULL)>value="$name" <?/MIVAR> NAME="name" size=80> 
  <p>

  <b>Address:</b><br>
  This field holds the COMPANY's surface mail address. Be sure to include all necessary zip codes or country codes. For example:<br>
  <pre>Institute of Neuroscience
  1254 University of Oregon
  Eugene,OR 97403-1254
  USA
  </pre>
  <TEXTAREA name=address WRAP=virtual rows=6 cols=70><?MIVAR COND=$(NC,$address,NULL)>$address<?/MIVAR></TEXTAREA>
  <p>

  <b>Front Office Phone:</b><br>
  This field should hold the front office (general contact) phone number for the company. Be sure to include relevant country codes.
  <INPUT type=text <?MIVAR COND=$(NC,$phone,NULL)>value="$phone"<?/MIVAR> name="phone" size="50">
  <p>

  <b>Front Office Fax:</b><br>
  This field should hold the front office (general contact) fax number for the company. Be sure to include relevant country codes.
  <INPUT type=text <?MIVAR COND=$(NC,$fax,NULL)>value="$fax" <?/MIVAR> name="fax" size="50">
  <p>


  <b>email:</b><br>
  This field should hold  the general contact email address for the company. 
  <INPUT type=text <?MIVAR COND=$(NC,$email,NULL)>value="$email" <?/MIVAR> name="email" size="80">
  <p>

  <b>URL:</b><br>
  This field should hold the address (URL) for the company's WWW site.
  <INPUT type=text <?MIVAR COND=$(NC,$url,NULL)>value="$url" <?/MIVAR> name="url" size="80">
  <p>

 
  <HR width=80%>
  <p>
  <input type=submit name="s_done" value="SUBMIT">
  <input type=hidden name=MIval value=aa-process-company.apg>

  </FORM>


<?/MIBLOCK>  <!-- ends cond authorize -->

<?MISQL SQL="select WebExplode(object,'') from webPages where ID='aa-htmlpagefooter.apg';">$1<?/MISQL>