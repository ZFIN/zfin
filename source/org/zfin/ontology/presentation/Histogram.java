package org.zfin.ontology.presentation;

import java.util.Map;

/**
 * ToDo: ADD DOCUMENTATION!
 */
public class Histogram<T> {

    private T entity;

    public Histogram(T entity) {
        this.entity = entity;
    }

/*
    private void addToHistogram(TermHistogramBean termUsageHistogram, Integer integer) {
        Integer termUsage = histogram.get(term);
        if (termUsage != null)
            histogram.put(term, termUsage + 1);
        else
            histogram.put(term, 1);
    }
*/

}
