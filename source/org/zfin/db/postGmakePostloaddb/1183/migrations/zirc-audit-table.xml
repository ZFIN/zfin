<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.0.xsd">

    <!--
      ZIRC audit trail. Until now the service only emitted log4j lines
      (`ZIRC_AUDIT ...`); curators need to query history without an SSH
      session. One row per user-driven change:

        - update : single field-path PATCH (entity kind = submission|mutation|assay)
        - create : POST that materialized a new row
        - delete : DELETE
        - upload : multipart attachment upload
        - delete-file : attachment removal

      old_value / new_value are JSONB so the existing Jackson serialization
      pipeline rounds-trips cleanly. path is the JSON pointer that the PATCH
      hit, or NULL for whole-row events (create/delete/upload).
    -->
    <changeSet author="rtaylor" id="zirc-audit-table">
        <sql>
            CREATE TABLE zirc.audit (
                ae_id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                ae_actor       TEXT NOT NULL,
                ae_entity_kind TEXT NOT NULL,
                ae_entity_id   TEXT NOT NULL,
                ae_action      TEXT NOT NULL,
                ae_path        TEXT,
                ae_old_value   JSONB,
                ae_new_value   JSONB,
                ae_at          TIMESTAMP NOT NULL DEFAULT now()
            );
            CREATE INDEX audit_entity_idx ON zirc.audit (ae_entity_kind, ae_entity_id, ae_at);
            CREATE INDEX audit_actor_idx  ON zirc.audit (ae_actor, ae_at);
        </sql>
    </changeSet>

</databaseChangeLog>
