<?xml version='1.0' encoding='UTF-8'?>
<project>
  <actions/>
  <description
  >Run the Jenkins job to load the GOA GAF files into the database.
Downloads from the following urls by default:

- ftp://ftp.ebi.ac.uk/pub/databases/GO/goa/ZEBRAFISH/goa_zebrafish.gaf.gz
- ftp://ftp.ebi.ac.uk/pub/databases/GO/goa/ZEBRAFISH/goa_zebrafish_isoform.gaf.gz
- ftp://ftp.ebi.ac.uk/pub/databases/GO/goa/ZEBRAFISH/goa_zebrafish_rna.gaf.gz

Those can be overridden by setting the environment variables:

- GOA_GAF_URL1
- GOA_GAF_URL2
- GOA_GAF_URL3

Steps:
      1. Download the GOA (Gene Ontology Annotation) file for zebrafish from EBI — a GAF (Gene Association Format) file containing GO annotations from multiple sources (UniProt, Ensembl, GO_Central, RNAcentral, etc.)
      2. Parse each line into a GafEntry — filtering out non-zebrafish entries and entries without valid taxon IDs
      3. Map UniProt IDs to ZFIN genes — looks up the entryId (e.g., Q5XJQ7) in db_link to find the associated ZFIN gene. This is where the "Gene not found" errors come from.
      4. Validate each annotation — checks evidence codes, inference fields, GO term validity (not obsolete, not in "Do Not Annotate" subset), publication mappings, protein binding rules, etc.
      5. Generate MarkerGoTermEvidence records — for each valid entry, creates an annotation linking a ZFIN gene to a GO term with evidence code, publication, and inferences
      6. Deduplicate — checks against existing annotations in the database. If a more specific annotation already exists, the new one is marked as "existing" and skipped.
      7. Batch add new annotations to the database
      8. Remove stale annotations — any previously-loaded annotations that aren't in the new GAF file are removed (these show up as "REMOVED" in the details file)
      9. Write reports — details file (removed/added/updated/errors/existing), summary file, and now the error summary

Input File Details:
      goa_zebrafish.gaf.gz — The main file. Contains GO annotations for zebrafish proteins (UniProt accessions). Each row maps a protein to a GO term with evidence. Sources include UniProt (IEA — electronic annotations), GO_Central (IBA — phylogenetic), IntAct
      (IPI — protein interactions), and manual curation from various groups. This is the bulk of the data — the annotations that map to genes via UniProt ID → db_link → ZFIN gene.

      goa_zebrafish_isoform.gaf.gz — Annotations for specific protein isoforms (splice variants). Same format as the main file, but the geneProductFormID field (column 17) contains isoform-specific accessions like UniProtKB:E9QI36-1, UniProtKB:E9QI36-2. These
      provide more granular annotations — e.g., isoform 1 of a protein localizes to the cytoplasm while isoform 2 localizes to the mitochondrion.

      goa_zebrafish_rna.gaf.gz — Annotations for non-coding RNA sequences. These use RNAcentral IDs (URS*) rather than UniProt accessions.

  </description>
  <keepDependencies>false</keepDependencies>
  <properties>
    <hudson.model.ParametersDefinitionProperty>
      <parameterDefinitions>
        <hudson.model.BooleanParameterDefinition>
          <name>RUN_MGTE_CLEANUP</name>
          <description>If checked, run cleanMarkerGoTermEvidenceDuplicatesTask after the load (and before the "after" snapshot is captured).</description>
          <defaultValue>false</defaultValue>
        </hudson.model.BooleanParameterDefinition>
        <hudson.model.BooleanParameterDefinition>
          <name>MGTE_CLEANUP_CSVS</name>
          <description>If checked (the default), the cleanup writes its four record-keeping CSVs to server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m-dbdiff/ under the deployment root -- the same directory this job already puts its before/after mgte_*.csv snapshots and mgte_dbdiff_*.xlsx comparison workbooks in, and which is archived with the build. The CSVs are the record of which duplicate annotations the cleanup deleted. Uncheck when this run is a step in a larger comparison and they are just bulky byproducts. Has no effect unless RUN_MGTE_CLEANUP is also checked.</description>
          <defaultValue>true</defaultValue>
        </hudson.model.BooleanParameterDefinition>
      </parameterDefinitions>
    </hudson.model.ParametersDefinitionProperty>
  </properties>
  <scm class="hudson.scm.NullSCM"/>
  <canRoam>true</canRoam>
  <disabled>false</disabled>
  <blockBuildWhenDownstreamBuilding>false</blockBuildWhenDownstreamBuilding>
  <blockBuildWhenUpstreamBuilding>false</blockBuildWhenUpstreamBuilding>
  <triggers>
    <hudson.triggers.TimerTrigger>
      <spec></spec>
    </hudson.triggers.TimerTrigger>
  </triggers>
  <concurrentBuild>false</concurrentBuild>
  <customWorkspace>$TARGETROOT</customWorkspace>
  <builders>
    <hudson.tasks.Shell>
      <command><![CDATA[
set -e
SQL=$SOURCEROOT/server_apps/DB_maintenance/gafLoad
OUT=$TARGETROOT/server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m-dbdiff

bash $SQL/mgte_snapshot.sh before "$OUT" --others GOA
]]></command>
    </hudson.tasks.Shell>
    <hudson.tasks.Ant plugin="ant@1.2">
      <targets>load-gaf-goa -DjobName=Load-GAF-GOA_m</targets>
      <buildFile>$SOURCEROOT/server_apps/DB_maintenance/build.xml</buildFile>
    </hudson.tasks.Ant>
    <hudson.tasks.Shell>
      <command><![CDATA[
set -e
# Optional: run the marker_go_term_evidence duplicate cleanup after the load.
# When enabled, the AFTER snapshot below reflects the post-cleanup state.
if [ "$RUN_MGTE_CLEANUP" = "true" ]; then
    echo "RUN_MGTE_CLEANUP=true -> running cleanMarkerGoTermEvidenceDuplicatesTask"
    cd $SOURCEROOT
    # The task's record-keeping CSVs are written by \copy with relative paths, so without a
    # directory they would land in $SOURCEROOT. -DcleanupCsvDir names one; omitting it skips them.
    if [ "$MGTE_CLEANUP_CSVS" = "false" ]; then
        CLEANUP_CSV_ARG=""
    else
        CLEANUP_CSV_ARG="-DcleanupCsvDir=$TARGETROOT/server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m-dbdiff"
    fi
    echo "MGTE_CLEANUP_CSVS=$MGTE_CLEANUP_CSVS -> ${CLEANUP_CSV_ARG:-(no CSVs)}"
    gradle cleanMarkerGoTermEvidenceDuplicatesTask $CLEANUP_CSV_ARG
else
    echo "RUN_MGTE_CLEANUP not true -> skipping cleanup"
fi
]]></command>
    </hudson.tasks.Shell>
    <hudson.tasks.Shell>
      <command><![CDATA[
set -e
# AFTER snapshot (post-load, and post-cleanup when RUN_MGTE_CLEANUP=true), then the diff.
SQL=$SOURCEROOT/server_apps/DB_maintenance/gafLoad
OUT=$TARGETROOT/server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m-dbdiff

bash $SQL/mgte_snapshot.sh after "$OUT" --others GOA
bash $SQL/mgte_csvdiff.sh        "$OUT" --others GOA
]]></command>
    </hudson.tasks.Shell>
  </builders>
  <publishers>
    <hudson.tasks.ArtifactArchiver>
      <artifacts>server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m/Load-GAF-GOA*,server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m-dbdiff/mgte_*.csv,server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m-dbdiff/mgte_*.xlsx</artifacts>
      <latestOnly>false</latestOnly>
      <allowEmptyArchive>true</allowEmptyArchive>
    </hudson.tasks.ArtifactArchiver>
    <hudson.plugins.emailext.ExtendedEmailPublisher plugin="email-ext@2.35.1">
      <recipientList></recipientList>
      <configuredTriggers>
        <hudson.plugins.emailext.plugins.trigger.SuccessTrigger>
          <email>
            <recipientList>@SUCCESS-RECIPIENT-LIST@</recipientList>
            <subject>$PROJECT_DEFAULT_SUBJECT</subject>
            <body>${FILE,path=&quot;server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m/Load-GAF-GOA_m_summary.txt&quot;}

${FILE,path=&quot;server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m/Load-GAF-GOA_m_error_summary.txt&quot;}

See full details at ${BUILD_URL}artifact/server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m/Load-GAF-GOA_m_details.txt</body>
            <sendToDevelopers>false</sendToDevelopers>
            <sendToRequester>false</sendToRequester>
            <includeCulprits>false</includeCulprits>
            <sendToRecipientList>true</sendToRecipientList>
            <attachmentsPattern></attachmentsPattern>
            <attachBuildLog>false</attachBuildLog>
            <compressBuildLog>false</compressBuildLog>
            <replyTo></replyTo>
            <contentType>project</contentType>
          </email>
        </hudson.plugins.emailext.plugins.trigger.SuccessTrigger>
        <hudson.plugins.emailext.plugins.trigger.FailureTrigger>
          <email>
            <recipientList>@FAILURE-RECIPIENT-LIST@</recipientList>
            <subject>$PROJECT_DEFAULT_SUBJECT</subject>
            <body>${FILE,path=&quot;server_apps/DB_maintenance/gafLoad/Load-GAF-GOA_m/Load-GAF-GOA_m_errors.txt&quot;}</body>
            <sendToDevelopers>false</sendToDevelopers>
            <sendToRequester>false</sendToRequester>
            <includeCulprits>false</includeCulprits>
            <sendToRecipientList>true</sendToRecipientList>
            <attachmentsPattern></attachmentsPattern>
            <attachBuildLog>false</attachBuildLog>
            <compressBuildLog>false</compressBuildLog>
            <replyTo></replyTo>
            <contentType>project</contentType>
          </email>
        </hudson.plugins.emailext.plugins.trigger.FailureTrigger>
      </configuredTriggers>
      <contentType>text/plain</contentType>
      <defaultSubject>[Jenkins] ${PROJECT_NAME}: ${BUILD_STATUS}: [${INSTANCE}]</defaultSubject>
      <defaultContent>$DEFAULT_CONTENT</defaultContent>
      <attachmentsPattern></attachmentsPattern>
      <presendScript></presendScript>
      <attachBuildLog>false</attachBuildLog>
      <compressBuildLog>false</compressBuildLog>
      <replyTo>${DB_OWNER}@zfin.org</replyTo>
      <saveOutput>false</saveOutput>
    </hudson.plugins.emailext.ExtendedEmailPublisher>
  </publishers>
  <buildWrappers>
      <hudson.plugins.build__timeout.BuildTimeoutWrapper plugin="build-timeout@1.20">
          <strategy class="hudson.plugins.build_timeout.impl.AbsoluteTimeOutStrategy">
              <timeoutMinutes>300</timeoutMinutes>
          </strategy>
          <operationList>
              <hudson.plugins.build__timeout.operations.AbortOperation/>
          </operationList>
      </hudson.plugins.build__timeout.BuildTimeoutWrapper>
  </buildWrappers>
</project>
