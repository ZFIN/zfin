
NOTES:

To further generate a wsdl file:

./generateStubs.sh 
<ant deploy should automatically build with these new jar files>

The files in bin rely on the jar files in lib, otherweise I would have moved them into Common/lib/Java.  The scripts and jars were taken directly from the jaxws-ri site.  


